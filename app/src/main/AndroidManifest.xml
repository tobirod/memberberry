<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newton.tr.member">

    <application
        android:allowBackup="true"
        android:name="com.newton.tr.member.app.App"
        android:icon="@mipmap/ic_launcher_member"
        android:label="MemberBerry"
        android:roundIcon="@mipmap/ic_launcher_member"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.newton.tr.member.activities.MainActivity"
            android:label="... member?"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>