<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/plum</item>
        <item name="colorPrimaryDark">@color/grape</item>
        <item name="colorAccent">@color/grape</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Base.Widget.Design.TabLayout" parent="android:Widget" tools:override="true">
        <item name="tabBackground">@drawable/tab_background</item>
        <item name="tabIndicatorColor">@color/pastelOrange</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabTextAppearance">@style/tabTextCustomStyle</item>
    </style>

    <style name="tabTextCustomStyle" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/snaphand</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="customRadioButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:textColor">@color/plum</item>
        <item name="buttonTint">@color/plum</item>
        <item name="android:fontFamily">@font/komika_text</item>
        <item name="android:textSize">15sp</item>
    </style>

</resources>
